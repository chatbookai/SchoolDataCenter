import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.dandian.dc.app',
  appName: 'Dandian DataCenter',
  webDir: 'out',
  "bundledWebRuntime": true
};

export default config;
